{
  "name": "agentic-browser-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm run build",
    "build:tauri": "cd backend/src-tauri && cargo tauri build"
  }
}
